import { IListTodoContext } from "../types/todo";

export const addList: IListTodoContext['addList'] = (title) => {

}

export const removeList: IListTodoContext['removeList'] = (id) => {

}

export const toggleList: IListTodoContext['toggleList'] = (id) => {
}

